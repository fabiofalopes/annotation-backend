// Container Types (must match backend schema)
export const CONTAINER_TYPES = ['chat_rooms', 'documents'] as const;

// Project Types (must match backend schema)
export const PROJECT_TYPES = ['chat_disentanglement', 'text_classification', 'other'] as const; 